# -*- mode: ruby -*-
# vi: set ft=ruby :


VAGRANTFILE_API_VERSION = "2"

Vagrant.configure("2") do |config|
  <% @systems.each do |system| %>
  config.vm.define "<%= system.id %>" do |<%= system.id %>|
    <%= system.id %>.vm.box = "<%= system.base %>"
  end
  <% end %>
end